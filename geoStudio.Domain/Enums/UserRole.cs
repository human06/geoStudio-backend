namespace geoStudio.Domain.Enums;

/// <summary>Defines the roles a user can have within a business team.</summary>
public enum UserRole
{
    Admin = 0,
    TeamLead = 1,
    Analyst = 2,
    Viewer = 3
}
